## Context
The approved rollout replaces duplicated process, not product requirements.

## Goals / Non-Goals
Use one change record, recoverable local work and reviewed exact publication.
No product changes, no new framework, no remote protection changes or force push.

## Decisions
The active contract is docs/agent-workflow.md after hash-verified activation.
The optional user-policy copies are reviewed here but installed only after old-policy
delivery. The preflight checks mechanical identities and evidence digests; independent
review origin and evidence truth remain independent judgement, not JSON authentication.
Native archive cannot reopen active names: use the authorized lightweight closeout.

## Risks / Trade-offs
Local preflight is not a server transaction lock. Environment observations must be
renewed when conditions change. Native OpenSpec probes are fixture evidence, not
product acceptance. External backups are required before activation.

## Migration Plan
Follow docs/ai-work-model-rollout.md under the prior contract. Existing staged tasks
are not converted. Disable activation and restore verified external backups to roll
back; revert published project policy through an audited normal delivery.
