## 1. Baseline and safe regression

- [x] 1.1 Verify base/rules/ON-OFF source mapping and run baseline production/location/Network build and tests; retain command exits.
- [x] 1.2 After readiness, add the minimal behavior-preserving selection seam and safe portable regression; demonstrate expected root mismatch with all paths temporary.

## 2. P1 path selection

- [x] 2.1 Select portable/cache between explicit injection and unchanged default; verify the same red assertion and actual Network preparation pass.
- [x] 2.2 Verify enabled/disabled/reapply/failure/abandon/abort/consumed-token/cleanup containment with temporary sentinels and unchanged Network behavior.

## 3. Product and regression validation

- [ ] 3.1 Run W1/W2/W3.1/W3.2 and affected configuration/Network regressions, separate ON/OFF production builds, and inspect actual target/link isolation.
- [x] 3.2 Complete the ordinary-startup path/instance safety matrix before launch; then record real portable initialization, main window and normal exit, or a specific unresolved unsafe path without launching.
- [x] 3.3 Update focused contract documentation and P1 verification record, validate OpenSpec and full diff, and retain exact candidate/evidence identity separately from W3.2.
